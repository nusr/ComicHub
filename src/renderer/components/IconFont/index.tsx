import { Icon } from 'antd';
import { IconProps } from 'antd/lib/icon';
import React from 'react';

const scriptUrl = '';

const IconFont: React.FunctionComponent<IconProps> = Icon.createFromIconfontCN({
  scriptUrl,
});
export default IconFont;
